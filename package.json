{
  "name": "portfolio",
  "version": "0.1.0",
  "homepage": "https://caleblemoine.dev",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "react-typist": "^2.0.5"
  },
  "scripts": {
    "start": "npx react-scripts start",
    "build": "npx react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "[ -d docs ] && rm -rf ./docs; npm run build && mv ./build ./docs && cp ./CNAME ./docs/ && git add . && git commit -m 'update docs' && git push"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
